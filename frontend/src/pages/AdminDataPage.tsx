import { BarChart3, ClipboardList, MessageSquare, Users } from 'lucide-react'

export function AdminDataPage({ kind }: { kind: 'predictions' | 'feedback' }) { const feedback = kind === 'feedback'; return <section className="admin-page"><div className="section-head"><div><p className="eyebrow">Admin / {feedback ? 'Feedback' : 'Predictions'}</p><h1>{feedback ? 'Farmer feedback' : 'Prediction monitoring'}</h1></div><span className="admin-badge">{feedback ? <MessageSquare size={14} /> : <BarChart3 size={14} />} Live workspace</span></div><div className="admin-metrics"><Metric icon={feedback ? <MessageSquare /> : <ClipboardList />} label={feedback ? 'Feedback received' : 'Predictions logged'} value="--" /><Metric icon={<Users />} label={feedback ? 'Helpful responses' : 'Low-confidence cases'} value="--" /><Metric icon={<BarChart3 />} label="Needs review" value="--" /></div><div className="empty-chart"><p className="eyebrow">{feedback ? 'Feedback ledger' : 'Prediction stream'}</p><h3>Data will appear as the monitoring API grows</h3><p>This surface is connected to the Stage 11 records and reserved for the admin metrics service.</p></div></section> }
function Metric({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="admin-metric"><span>{icon}</span><p>{label}</p><strong>{value}</strong></div> }
