name: smart_farming_mobile
description: Camera-first offline-ready Smart Farming client.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.4
  http: ^1.3.0
  image_picker: ^1.1.2
  shared_preferences: ^2.5.3
  flutter_tts: ^4.2.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
