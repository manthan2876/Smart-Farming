# Smart Farming Backend — Python Dependencies
#
# Install with:
#   pip install -r requirements.txt

# Core
numpy>=1.24.0
pyyaml>=6.0

# Computer Vision (preprocessing + severity)
opencv-python>=4.7.0

# Deep Learning (crop ID + disease classification)
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# Augmentations (used in inference transforms)
albumentations>=1.3.0

# Pest Detection (YOLOv8)
ultralytics>=8.0.0

# Web Server (Stage 8 — FastAPI, not needed for standalone smoke test)
# fastapi>=0.100.0
# uvicorn[standard]>=0.22.0
# python-multipart>=0.0.6
